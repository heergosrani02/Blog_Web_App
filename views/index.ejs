<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Blogs</title>
</head>
<body>
    <h1>Travel Blogs</h1>

    <% data.forEach((e) => { %>
        <h2><%= e.title %></h2>
        <h3><%= e.date %></h3>
        <a href="/page/<%= e.id %>"> Read More </a>
    <% }) %>

</body>
</html>